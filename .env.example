# =============================================================================
# SECURITY WARNING
# =============================================================================
# This file is a TEMPLATE. Never commit actual secrets to version control.
# Copy this file to .env.local and fill in your actual credentials.
# .env.local is already in .gitignore and should never be committed.
#
# After copying, generate strong secrets using:
#   openssl rand -base64 32

# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
NODE_ENV="development"

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection URL with connection pooling (required)
POSTGRES_PRISMA_URL="postgresql://user:password@host:port/database?pgbouncer=true"

# PostgreSQL direct connection URL for migrations (required)
POSTGRES_URL_NON_POOLING="postgresql://user:password@host:port/database"

# =============================================================================
# AUTHENTICATION (NextAuth.js)
# =============================================================================
# Base URL of your application (required)
NEXTAUTH_URL="http://localhost:3000"

# Secret key for NextAuth.js session encryption (required)
# Generate with: openssl rand -base64 32
# NEVER use a weak or default secret in production!
NEXTAUTH_SECRET="REPLACE_WITH_STRONG_SECRET_MINIMUM_32_CHARS"

# =============================================================================
# GITHUB OAUTH CONFIGURATION
# =============================================================================
# Create OAuth app at: https://github.com/settings/developers
GITHUB_ID="your-github-oauth-client-id"
GITHUB_SECRET="your-github-oauth-client-secret"

# =============================================================================
# UPLOADTHING CONFIGURATION
# =============================================================================
# Get your keys at: https://uploadthing.com/dashboard
UPLOADTHING_SECRET="your-uploadthing-secret"
UPLOADTHING_APP_ID="your-uploadthing-app-id"

# Alternative token-based authentication (if using tokens instead of secret/app-id)
# UPLOADTHING_TOKEN="your-uploadthing-token"

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# GitHub username of the blog owner (always has admin access)
OWNER_GITHUB_LOGIN="italicninja"

# =============================================================================
# DEVELOPMENT AUTHENTICATION BYPASS
# =============================================================================
# SECURITY: Only enable this in local development!
# Set to 'true' to enable dev authentication bypass
# This allows passwordless login as 'developer' user
# NEVER set this to 'true' in production or on Vercel!
ENABLE_DEV_AUTH="false"

# =============================================================================
# OPTIONAL: VERCEL DEPLOYMENT (Auto-populated by Vercel)
# =============================================================================
# These are automatically set by Vercel - do not set manually
# VERCEL="1"
# VERCEL_ENV="production"
# VERCEL_URL=""
# VERCEL_GIT_COMMIT_SHA=""
